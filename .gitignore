# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# ── Virtual environments ───────────────────────────────
venv/
.venv/
env/
.env/

# ── IDE / editor ───────────────────────────────────────
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# ── Data (large files — download separately) ───────────
data/fer2013/
data/pins_face_recognition/
data/CelebAMask-HQ/
data/CelebA-HQ-img/
data/*.csv
data/*.zip
data/*.tar.gz

# ── Pretrained weights ─────────────────────────────────
pretrained/*.pth
pretrained/*.onnx
pretrained/*.pt
pretrained/*.pkl
pretrained/**/*.pth
pretrained/**/*.onnx
pretrained/**/*.ckpt
pretrained/**/*.bin
!pretrained/.gitkeep

# ── Third-party repos (cloned at runtime) ──────────────
third_party/

# ── Cache / intermediate outputs ───────────────────────
cache/
*.npy
*.npz
data/synthetic/

# ── Results (keep CSVs, plots, tables; ignore run dirs) ─
results/run_*/
results/**/tb_logs/
!results/.gitkeep

# ── Reports (generated) ───────────────────────────────
reports/*.png
reports/*.pdf

# ── Coverage ───────────────────────────────────────────
.coverage
htmlcov/

# ── Jupyter ────────────────────────────────────────────
.ipynb_checkpoints/

# ── OS ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ───────────────────────────────────────────────
*.log
outputs/         # Hydra default output dir
multirun/        # Hydra multirun dir
